[package]
name = "bevy_saga"
version = "0.1.0"
edition = "2024"

[lib]

[dependencies]
bevy = { version = "0.16", default-features = false }

[dev-dependencies]
bevy_saga_macros = { path = "../bevy_saga_macros" }